root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/auth-service.exe ./main.go"
  bin = "tmp/auth-service.exe"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 200

[log]
  time = true

